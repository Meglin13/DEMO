<Page x:Class="DemoTrannig.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DemoTrannig.Pages"
      mc:Ignorable="d"
      Title="LoginPage">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Height="400" Width="350" Background="#FF76E383" >
            <Label Content="Авторизация" FontSize="25" HorizontalAlignment="Center"/>
            <Label Content="Логин"/>
            <TextBox x:Name="LoginTextBox"/>
            <Label Content="Пароль"/>
            <PasswordBox x:Name="PasswordBox"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20 0 0">
                <Button Content="Вход" HorizontalAlignment="Center" Name="LoginButton" Click="LoginButton_Click"/>
                <Button Content="Войти как гость" HorizontalAlignment="Right" Name="GuestEnterButton" Click="GuestEnterButton_Click"/>
            </StackPanel>
        </StackPanel>
        
    </Grid>
</Page>
